import React from 'react';
import { motion } from 'motion/react';

interface LoadingSpinnerProps {
  fullPage?: boolean;
  small?: boolean;
}

const LoadingSpinner: React.FC<LoadingSpinnerProps> = ({ fullPage = false, small = false }) => {
  return (
    <div className={`flex flex-col items-center justify-center ${fullPage ? 'fixed inset-0 z-[200] bg-white dark:bg-black' : small ? 'w-fit h-fit' : 'py-20 w-full'}`}>
      <div className={`relative ${small ? 'w-8 h-8' : 'w-24 h-24'}`}>
        {/* Outer Rotating Star */}
        <motion.div
          animate={{ rotate: 360 }}
          transition={{ duration: 8, repeat: Infinity, ease: "linear" }}
          className="absolute inset-0"
        >
          <svg viewBox="0 0 100 100" className={`w-full h-full fill-none ${small ? 'stroke-[6]' : 'stroke-[3]'}`}>
            {/* 8-pointed star (Rub el Hizb style) */}
            <path
              d="M50 5 L63 37 L95 50 L63 63 L50 95 L37 63 L5 50 L37 37 Z"
              className="stroke-orange-500/30 dark:stroke-orange-500/30"
            />
            <path
              d="M50 5 L63 37 L95 50 L63 63 L50 95 L37 63 L5 50 L37 37 Z"
              transform="rotate(45 50 50)"
              className="stroke-orange-500/30 dark:stroke-orange-500/30"
            />
          </svg>
        </motion.div>

        {/* Inner Pulsing Geometric Pattern */}
        <motion.div
          animate={{ 
            scale: [1, 1.1, 1],
            rotate: [0, -90, -180, -270, -360]
          }}
          transition={{ 
            duration: 4, 
            repeat: Infinity, 
            ease: "easeInOut" 
          }}
          className={`absolute ${small ? 'inset-1' : 'inset-4'}`}
        >
          <svg viewBox="0 0 100 100" className="w-full h-full">
            <path
              d="M50 10 L60 40 L90 50 L60 60 L50 90 L40 60 L10 50 L40 40 Z"
              className="fill-green-600 dark:fill-orange-600"
            />
            <circle cx="50" cy="50" r="8" className="fill-yellow-500" />
          </svg>
        </motion.div>

        {/* Decorative Dots */}
        {!small && [0, 45, 90, 135, 180, 225, 270, 315].map((angle) => (
          <motion.div
            key={angle}
            className="absolute w-1.5 h-1.5 rounded-full bg-yellow-500"
            style={{
              top: '50%',
              left: '50%',
              transform: `rotate(${angle}deg) translateY(-42px)`,
            }}
            animate={{ opacity: [0.3, 1, 0.3] }}
            transition={{ duration: 2, repeat: Infinity, delay: angle / 360 }}
          />
        ))}
      </div>

      {!small && (
        <div className="mt-8 text-center space-y-2">
          <motion.p 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            className="text-stone-900 dark:text-white font-serif tracking-widest text-sm uppercase"
          >
            Loading...
          </motion.p>
          <motion.p 
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.3 }}
            className="text-orange-600 dark:text-orange-400 font-urdu text-xl"
            dir="rtl"
          >
            لوڈنگ جاری ہے...
          </motion.p>
        </div>
      )}
    </div>
  );
};

export default LoadingSpinner;
